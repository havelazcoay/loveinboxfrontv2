  const navigate = useNavigate();

  const logOut = (e) =>{
    if(auth){
      auth.signOut();
      navigate("/")
    }
    else{
      console.log("Usted no ha iniciado")
    }
  };





  <button onClick={logOut}>Log Out</button>